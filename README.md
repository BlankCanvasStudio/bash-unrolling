# bashunroll: A python library to de-obfuscate and return a command trace for bash files

TODO: README 

Functions:

basic_unroll(nodes, function dict, command aliases)
replacement_unroll(nodes, variable dict)
